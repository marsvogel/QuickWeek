version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    # Bundle all GitHub Actions bumps into one PR to keep the noise down.
    groups:
      actions:
        patterns:
          - "*"
